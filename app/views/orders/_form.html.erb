<%= form_with(model: order) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_no %>
    <%= form.number_field :order_no %>
  </div>

  <div class="field">
    <%= form.label :date_of_Order %>
    <%= form.date_select :date_of_Order %>
  </div>

  <div class="field">
    <%= form.label :order_status %>
    <%= form.text_field :order_status %>
  </div>

  <div class="field">
    <%= form.label :billing_add %>
    <%= form.text_field :billing_add %>
  </div>

  <div class="field">
    <%= form.label :shipping_add %>
    <%= form.text_field :shipping_add %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
